# @symphony-ai/central-ds-variables

Design tokens and CSS variables for the Central Design System.

> **Note:** This package is consumed internally by the umbrella package `@symphony-ai/central-ds`. End users should install the umbrella package instead.

## Migration

Migrate your existing `central-ds-variables` source code into the `src/` directory of this package. Update exports in `src/index.ts` accordingly.
