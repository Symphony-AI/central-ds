# @symphony-ai/central-ds-react

React components for the Central Design System.

> **Note:** This package is consumed internally by the umbrella package `@symphony-ai/central-ds`. End users should install the umbrella package instead.

## Migration

Migrate your existing `central-ds-react` source code into the `src/` directory of this package. Update exports in `src/index.ts` accordingly.
